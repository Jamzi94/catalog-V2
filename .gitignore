# Scrape cache directories
.scrape_cache/
.scrape_cache_api/
.scrape_cache_superpsx/
.scrape_cache_sizes/

# Incremental manifests : VOLONTAIREMENT versionnés (commit entre runs)
# pour piloter le vrai incrémental (champ 'modified' + last_run). NE PAS ignorer.
# .scrape_manifest.json
# .scrape_manifest_api.json
# .scrape_manifest_superpsx.json

# Intermediate / temporary files
ps5-catalog.fresh.json
superpsx-ps5.fresh.json
exfat-ps5.fresh.json
ps5-catalog.previous.json
ps5-catalog.merged.json
ps5-catalog.fresh.warnings.txt

# Metrics & diff reports (regenerated each run)
scrape-metrics.json
diff-report.json

# Python
__pycache__/
*.pyc
*.pyo
.venv/

# OS
.DS_Store
Thumbs.db

# Outillage local (Serena) — ne doit pas etre publie
.serena/
